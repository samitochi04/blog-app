// src/environments/environment.ts

export const environment = {
    production: false,  // This indicates this is not a production build
    apiUrl: 'http://localhost:3000/api',  // API URL for development
    authTokenKey: 'auth-token',  // Key for storing JWT token in localStorage
  };
  